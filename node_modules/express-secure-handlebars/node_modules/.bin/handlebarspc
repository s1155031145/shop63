#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../context-parser-handlebars/bin/handlebarspc" "$@"
  ret=$?
else 
  node  "$basedir/../context-parser-handlebars/bin/handlebarspc" "$@"
  ret=$?
fi
exit $ret
